
<html>
<head>
  <style>
    .gap-10 {
      width:100%;
      height:10px;
    }
    .gap-20 {
      width:100%;
      height:20px;
    }
    .gap-30 {
      width:100%;
      height:30px;
    }
  </style>
</head>
<body>


<h4>Below are the Tesco stores near you!</h4>
<%= form_tag "/details" do %>

<table>
  <thead>
  <tr>
    <th>Branch name</th>
    <th>Contact number</th>
    <th>Address</th>
    <th>Distance</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <!-- this div will give a gap of 10px -->
  <div class="gap-20"></div>


  <% @stores.each_with_index do |item, i| %>
    <tr>
      <td><%= item["name"] %></td>
      <td><%= item["phone"] %></td>
      <td><%= item["door_number"] %>, <%= item["address"]  %>, <%= item["town"]  %>, <%= item["postcode"]  %>  </td>
      <td><%= item["distance"]  %> miles </td>
      <td><%= link_to 'Show Details', details_path(item), :method => :post %> </td>

    </tr>

  <% end %>
  </tbody>
</table>



  <head>
    <style>
      /* Set the size of the div element that contains the map */
      #map {
        height: 400px;  /* The height is 400 pixels */
        width: 80%;  /* The width is the width of the web page */
      }
    </style>

  <body>



  <head>
    <style>
      /* Set the size of the div element that contains the map */
      #map {
        height: 400px;  /* The height is 400 pixels */
        width: 80%;  /* The width is the width of the web page */
      }
    </style>
  </head>

  <body>

  <!-- this div will give a gap of 10px -->
  <div class="gap-20"></div>


  <h5>Check them on maps below</h5>
  <!--The div element for the map -->
  <div id="map"></div>
  <script>
      // Initialize and add the map
      function initMap() {
          // The location of Uluru
          var uluru = {lat: 53.34247942306117, lng: -6.2571363151073465};
          var uluru1 = {lat: 55.6117, lng: -8.51073465};
          // The map, centered at Uluru
          var map = new google.maps.Map(
              document.getElementById('map'), {zoom: 4, center: uluru});
          // The marker, positioned at Uluru
          var marker = new google.maps.Marker({position: uluru, map: map});
          var marker = new google.maps.Marker({position: uluru1, map: map});

      }
  </script>
  <!--Load the API from the specified URL
  * The async attribute allows the browser to render the page while the API loads
  * The key parameter will contain your own API key (which is not needed for this tutorial)
  * The callback parameter executes the initMap() function
  -->
  <script async defer
          src="https://maps.googleapis.com/maps/api/js?key=AIzaSyD0amoqetefKmBzelsLdJMlFzuEaC1qTEo&callback=initMap">
  </script>
  </body>


<% end %>

<td><%= link_to 'Back', check_path %></td>