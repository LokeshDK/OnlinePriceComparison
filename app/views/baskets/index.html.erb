<p id="notice"><%= notice %></p>

<h1>Baskets</h1>


<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @baskets.each do |basket| %>
      <tr>
        <td><%= basket.name %></td>
        <td><%= basket.status %></td>
        <table>

        </table>
        </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Basket', new_basket_path %>
